apiVersion: 1

providers:
  - name: Default
    folder: ''
    type: file
    disableDeletion: false   # разрешить удалять через UI
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/dashboards
      foldersFromFilesStructure: true